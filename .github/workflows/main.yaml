name: My First Workflow

on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Show push
        run: echo "Push successful"
      - name: Show name
        run: ls
  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Show test
        run: echo " tested"